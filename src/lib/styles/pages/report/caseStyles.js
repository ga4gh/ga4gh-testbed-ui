import { makeStyles } from '@mui/styles';

const caseStyles = makeStyles((theme) => ({
    div: {
        paddingLeft: 100,
        paddingTop: 10,
        paddingBottom: 10,
    }
}))

export default caseStyles;