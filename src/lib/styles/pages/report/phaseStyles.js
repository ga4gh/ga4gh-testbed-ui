import { makeStyles } from '@mui/styles';

const phaseStyles = makeStyles((theme) => ({
    div: {
        paddingTop: 25,
        paddingBottom: 25,
        borderBottom: `1px solid #909090`
    }
}))

export default phaseStyles;